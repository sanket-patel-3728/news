class ArticleModel {
  String author, title, desc, url, urlToImage, content;

  ArticleModel(
      {this.desc,
      this.title,
      this.url,
      this.author,
      this.content,
      this.urlToImage});
}
